# Production Readiness Checklist

## ✅ Критичные блокеры (ИСПРАВЛЕНО)

### F-001: Ledger для баланса
**Статус:** ✅ ИСПРАВЛЕНО

**Проблема:** Баланс хранился как число в `wallets.balance` → гонки и расхождения.

**Решение:**
- Создана таблица `ledger` (append-only журнал)
- Баланс = `SUM(ledger.amount)` — источник истины
- Типы операций: `credit`, `debit`, `refund`
- Атомарные транзакции с `BEGIN IMMEDIATE`
- Миграция из старой `wallets` в `ledger`

**Файлы:**
- `database/models.py` — новая модель с ledger
- `tests/test_ledger.py` — unit тесты

---

### F-002: Атомарность списания
**Статус:** ✅ ИСПРАВЛЕНО

**Проблема:** Списание происходило без транзакций → возможность отрицательного баланса.

**Решение:**
- Метод `reserve_balance()` с блокировкой
- Проверка баланса внутри транзакции
- Rollback при недостаточных средствах
- Резервирование → списание → refund (если ошибка)

**Файлы:**
- `database/models.py` — методы `reserve_balance()`, `charge_reserved_balance()`, `refund_balance()`
- `services/rate_limiter.py` — утилита `check_balance_and_reserve()`

---

### F-003: Идемпотентность webhook
**Статус:** ✅ ИСПРАВЛЕНО

**Проблема:** Повторные webhook от YooKassa → повторное зачисление.

**Решение:**
- Уникальный индекс на `payments.provider_payment_id`
- Проверка статуса перед обработкой
- State machine: `pending` → `paid` → `refunded`
- Возврат 200 OK для уже обработанных платежей

**Файлы:**
- `handlers/webhook.py` — идемпотентный обработчик
- `database/models.py` — уникальный индекс
- `tests/test_payments.py` — тесты идемпотентности

---

### F-004: Асинхронная очередь задач
**Статус:** ✅ ИСПРАВЛЕНО

**Проблема:** Синхронная генерация → таймауты, дубли, блокировка бота.

**Решение:**
- Redis + RQ для очереди задач
- Workers для обработки задач в фоне
- Fallback на синхронный режим (если Redis недоступен)
- Статусы: `pending` → `processing` → `completed`/`failed`

**Файлы:**
- `services/queue.py` — интеграция с RQ
- `workers/image_worker.py` — worker для изображений
- `workers/video_worker.py` — worker для видео

---

### F-005: Job-модель со статусами
**Статус:** ✅ ИСПРАВЛЕНО

**Проблема:** Нет отслеживания статуса задач → пользователь не знает прогресс.

**Решение:**
- Расширенная таблица `jobs` с полями:
  - `status`: pending, processing, completed, failed, cancelled
  - `progress`: 0-100%
  - `cost_estimate`, `cost_actual`
  - `started_at`, `completed_at`
- Методы для обновления статуса и отмены

**Файлы:**
- `database/models.py` — модель jobs
- `workers/*.py` — обновление статусов

---

### F-006: Защита от конкурентных запросов
**Статус:** ✅ ИСПРАВЛЕНО

**Проблема:** Несколько запросов одновременно → отрицательный баланс.

**Решение:**
- Rate limiting (10 запросов/минуту)
- Ограничение активных задач (3 на пользователя)
- Middleware для проверки лимитов
- In-memory хранилище (для production → Redis)

**Файлы:**
- `services/rate_limiter.py` — RateLimiter + middleware
- `tests/test_rate_limiter.py` — unit тесты

---

## 🔧 Дополнительные улучшения

### Единый интерфейс для AI провайдеров
**Статус:** ✅ ДОБАВЛЕНО

**Описание:**
- Базовый класс `BaseAIProvider`
- Интерфейсы `ImageProvider`, `VideoProvider`
- Метод `generate_with_retry()` с повторными попытками
- Единообразная обработка ошибок

**Файлы:**
- `services/base_provider.py`

---

### Unit и Integration тесты
**Статус:** ✅ ДОБАВЛЕНО

**Покрытие:**
- Ledger (баланс, резервирование, история)
- Платежи (идемпотентность, статусы)
- Rate limiter (лимиты, окна, изоляция)

**Файлы:**
- `tests/test_ledger.py`
- `tests/test_payments.py`
- `tests/test_rate_limiter.py`

**Запуск:**
```bash
pytest tests/ -v
```

---

## 📋 Чеклист перед деплоем

### Обязательные шаги

- [ ] **Настроить Redis**
  - Установить Redis Server
  - Указать `REDIS_URL` в `.env`
  - Запустить RQ worker: `rq worker --url redis://localhost:6379`

- [ ] **Настроить webhook YooKassa**
  - Получить публичный URL (ngrok, домен)
  - Указать в YooKassa: `https://your-domain.com/webhook/yookassa`
  - Проверить доступность эндпоинта

- [ ] **Настроить переменные окружения**
  - `BOT_TOKEN` — токен бота
  - `ADMIN_IDS` — ID администраторов
  - `NANO_BANANA_API_KEY` — ключ API
  - `KLING_API_KEY` — ключ API
  - `YOOKASSA_SHOP_ID`, `YOOKASSA_SECRET_KEY`
  - `REDIS_URL` — URL Redis
  - `DATABASE_PATH` — путь к БД

- [ ] **Запустить миграцию БД**
  ```bash
  python3 -c "import asyncio; from database.models import db; asyncio.run(db.init_db())"
  ```

- [ ] **Запустить тесты**
  ```bash
  pytest tests/ -v
  ```

- [ ] **Настроить логирование**
  - Уровень логов: INFO (production), DEBUG (dev)
  - Ротация логов
  - Мониторинг ошибок (Sentry, Rollbar)

- [ ] **Настроить резервное копирование**
  - Бэкап БД (ежедневно)
  - Бэкап конфигурации
  - Тестирование восстановления

### Рекомендуемые шаги

- [ ] **Мониторинг и алерты**
  - Prometheus + Grafana
  - Алерты на ошибки
  - Метрики: RPS, latency, queue size

- [ ] **Rate limiting на уровне Redis**
  - Заменить in-memory на Redis
  - Использовать `redis-py` с TTL

- [ ] **CDN для ассетов**
  - S3 + CloudFront
  - TTL для изображений/видео
  - Автоматическая очистка старых файлов

- [ ] **Graceful shutdown**
  - Обработка SIGTERM
  - Завершение активных задач
  - Закрытие соединений

- [ ] **Health checks**
  - Эндпоинт `/health`
  - Проверка БД, Redis, API

---

## 🚀 Запуск в production

### 1. Установка зависимостей
```bash
pip3 install -r requirements.txt
```

### 2. Настройка окружения
```bash
cp .env.example .env
nano .env  # Заполнить все переменные
```

### 3. Инициализация БД
```bash
python3 -c "import asyncio; from database.models import db; asyncio.run(db.init_db())"
```

### 4. Запуск Redis (если не запущен)
```bash
redis-server
```

### 5. Запуск RQ worker (в отдельном терминале)
```bash
rq worker --url redis://localhost:6379
```

### 6. Запуск бота
```bash
python3 main.py
```

### 7. Запуск webhook сервера (если нужен)
Webhook сервер запускается автоматически в `main.py`

---

## 🔍 Мониторинг

### Логи
```bash
tail -f bot.log
```

### Очередь задач (RQ)
```bash
rq info --url redis://localhost:6379
```

### Баланс пользователя
```python
from database.models import db
import asyncio

async def check_balance(user_id):
    balance = await db.get_balance(user_id)
    print(f"Баланс пользователя {user_id}: {balance} ₽")

asyncio.run(check_balance(123456789))
```

### История операций
```python
from database.models import db
import asyncio

async def check_history(user_id):
    history = await db.get_ledger_history(user_id, limit=10)
    for entry in history:
        print(f"{entry['created_at']}: {entry['type']} {entry['amount']} ₽ - {entry['description']}")

asyncio.run(check_history(123456789))
```

---

## ⚠️ Известные ограничения

1. **Rate limiter in-memory**
   - Не сохраняется при перезапуске
   - Не работает при horizontal scaling
   - **Решение:** Использовать Redis

2. **Webhook требует публичный URL**
   - Для dev: ngrok, localtunnel
   - Для production: домен с SSL

3. **Redis опционален**
   - Без Redis задачи выполняются синхронно
   - Может быть медленно для видео

4. **Нет автоматической очистки ассетов**
   - Изображения/видео накапливаются
   - **Решение:** Cron job для очистки старых файлов

---

## 📞 Поддержка

При возникновении проблем:
1. Проверить логи: `tail -f bot.log`
2. Проверить статус Redis: `redis-cli ping`
3. Проверить очередь: `rq info`
4. Запустить тесты: `pytest tests/ -v`

Для отчетов об ошибках: создать issue в репозитории.
