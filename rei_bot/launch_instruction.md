 # 🚀 Инструкция по запуску Telegram-бота «РЭИ»

Это руководство поможет вам развернуть и запустить бота на вашем сервере.

## 1. Предварительные требования

-   Сервер под управлением Linux (рекомендовано Ubuntu 22.04+).
-   Установленный Python 3.10 или выше (рекомендовано 3.11).
-   Доступ к командной строке сервера.

## 2. Получение исходного кода

Вы можете скачать архив с исходным кодом и распаковать его на сервере, либо использовать Git для клонирования репозитория.

```bash
# Пример распаковки архива
unzip rei_bot.zip
cd rei_bot
```

## 3. Установка зависимостей

Все необходимые библиотеки перечислены в файле `requirements.txt`. Установите их с помощью менеджера пакетов `pip`.

```bash
# Перейдите в директорию проекта
cd /path/to/rei_bot

# Установите зависимости
sudo pip3 install -r requirements.txt
```

## 4. Настройка конфигурации

Конфигурация бота управляется через переменные окружения, которые загружаются из файла `.env`.

1.  **Создайте файл `.env`:**

    Скопируйте файл-пример `.env.example`, чтобы создать свой конфигурационный файл.

    ```bash
    cp .env.example .env
    ```

2.  **Отредактируйте файл `.env`:**

    Откройте файл `.env` в любом текстовом редакторе и заполните его вашими данными.

    ```ini
    # Telegram Bot Configuration
    # Получите у @BotFather в Telegram
    BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

    # Admin Configuration
    # ID ваших Telegram-аккаунтов через запятую (без пробелов)
    # Узнать свой ID можно у @userinfobot
    ADMIN_IDS=123456789,987654321

    # API Keys
    # Ваши ключи для доступа к API нейросетей
    NANO_BANANA_API_KEY=your_nano_banana_api_key
    KLING_API_KEY=your_kling_api_key

    # Pricing (in rubles)
    # Цены на услуги в рублях (целые числа)
    IMAGE_GENERATION_PRICE=50
    IMAGE_EDIT_PRICE=50
    VIDEO_5SEC_PRICE=100
    VIDEO_10SEC_PRICE=200

    # Database
    # Путь к файлу базы данных SQLite. Можно оставить по умолчанию.
    DATABASE_PATH=./bot.db
    ```

## 5. Запуск бота

После того как все зависимости установлены и конфигурация настроена, вы можете запустить главного бота.

```bash
# Убедитесь, что вы находитесь в директории проекта
cd /path/to/rei_bot

# Запустите main.py
python3 main.py
```

Если все настроено правильно, в консоли вы увидите сообщение:

```
INFO:__main__:Бот запущен
```

Теперь бот активен и готов к работе в Telegram.

## 6. (Опционально) Запуск в фоновом режиме

Для того чтобы бот продолжал работать после закрытия терминала, рекомендуется запускать его с помощью менеджера процессов, такого как `systemd` или `supervisor`.

### Пример systemd-сервиса:

1.  Создайте файл юнита:

    ```bash
    sudo nano /etc/systemd/system/rei_bot.service
    ```

2.  Добавьте в него следующее содержимое, заменив `/path/to/rei_bot` на ваш реальный путь:

    ```ini
    [Unit]
    Description=REI Telegram Bot
    After=network.target

    [Service]
    User=ubuntu
    Group=www-data
    WorkingDirectory=/path/to/rei_bot
    ExecStart=/usr/bin/python3 /path/to/rei_bot/main.py
    Restart=always

    [Install]
    WantedBy=multi-user.target
    ```

3.  Включите и запустите сервис:

    ```bash
    sudo systemctl enable rei_bot.service
    sudo systemctl start rei_bot.service
    ```

4.  Проверить статус сервиса можно командой:

    ```bash
    sudo systemctl status rei_bot.service
    ```
