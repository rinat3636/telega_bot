# Changelog

## [v1.3] - 2026-02-11 - Автоматическая оплата через ЮКассу

### ✨ Новые возможности

#### 💳 Автоматическая оплата через ЮКассу

**Полная интеграция с платежной системой YooKassa:**

- ✅ **Автоматическое создание платежей** — бот создает платеж в ЮКассе и отправляет пользователю ссылку на оплату
- ✅ **Webhook для автоматического пополнения** — после успешной оплаты баланс пополняется автоматически без участия администратора
- ✅ **Ручная проверка статуса** — если webhook не настроен, пользователь может проверить статус платежа вручную кнопкой "🔄 Проверить оплату"
- ✅ **Предустановленные суммы** — быстрый выбор: 100, 500, 1000, 2000 ₽
- ✅ **Пользовательская сумма** — возможность ввести любую сумму от 100 до 15000 ₽
- ✅ **История платежей** — все платежи сохраняются в БД с информацией о статусе

#### 📊 Новая таблица в БД

Добавлена таблица `payments` для хранения информации о платежах.

### 📝 Новые файлы

- **`services/yookassa_payment.py`** — сервис для работы с YooKassa API
- **`handlers/webhook.py`** — обработчик webhook уведомлений от ЮКассы
- **`YOOKASSA_SETUP.md`** — подробная инструкция по настройке автоматической оплаты

### 🔧 Изменения в существующих файлах

- **`handlers/balance.py`** — полностью переработан с поддержкой автоматической оплаты
- **`database/models.py`** — добавлена таблица `payments` и методы для работы с платежами
- **`config.py`** — добавлены переменные окружения для YooKassa
- **`.env.example`** — добавлены примеры настроек YooKassa
- **`requirements.txt`** — добавлена зависимость `yookassa==2.3.0`
- **`README.md`** — обновлена документация с описанием автоматической оплаты

### 🎯 Как использовать

1. Настройте YooKassa (см. `YOOKASSA_SETUP.md`)
2. Пользователь отправляет `/pay`
3. Выбирает сумму или вводит свою
4. Переходит по ссылке и оплачивает
5. Баланс пополняется автоматически

### 🔒 Безопасность

- Секретный ключ YooKassa хранится в `.env` файле
- Все операции с платежами логируются
- Webhook проверяет подлинность уведомлений

### 💰 Ограничения

- **Минимальная сумма:** 100 ₽
- **Максимальная сумма:** 15000 ₽
- **Комиссия YooKassa:** обычно 2-3%

---

## [v1.2] - 2026-02-11 - Исправление логики цен

### 🔧 Исправлено

- Создан модуль `services/pricing.py` с единой функцией `video_price()`
- Цена 10 сек = VIDEO_5SEC_PRICE × VIDEO_10SEC_MULTIPLIER (по умолчанию ×2)
- Все кнопки и обработчики используют единую функцию

---

## [v1.1] - 2026-02-11

### 🔧 Критичные исправления

#### A) Добавлено подтверждение перед списанием средств

**Проблема:** Средства списывались сразу при отправке текста/фото без возможности отмены.

**Решение:**
- **Изображения (`handlers/images.py`):**
  - При отправке текста для генерации показывается экран подтверждения с ценой и балансом
  - При отправке фото для редактирования показывается экран подтверждения
  - Списание происходит только после нажатия кнопки "▶️ Запустить"
  - Добавлена кнопка "⬅️ Назад" для отмены операции

- **Видео (`handlers/videos.py`):**
  - После выбора модели, длительности и загрузки контента показывается экран подтверждения
  - Отображается: модель, длительность, описание, цена, баланс
  - Списание происходит только после нажатия "▶️ Запустить"
  - Добавлена возможность отмены на любом этапе

**Формат экрана подтверждения:**
```
🎨 Генерация изображения

Описание: [текст пользователя]

💰 Стоимость: 50 ₽
💳 Ваш баланс: 1000 ₽

Подтвердите запуск:
[▶️ Запустить] [⬅️ Назад]
```

#### B) Добавлен выбор модели Kling

**Проблема:** Модель Kling не выбиралась, использовалась захардкоженная модель `kling-v1`.

**Решение:**
- **Конфигурация (`config.py`, `.env.example`):**
  - Добавлена переменная окружения `KLING_MODELS`
  - Формат: `model_id:display_name` через запятую
  - По умолчанию: `kling-3.0:Kling 3.0,kling-2.6:Kling 2.6,kling-1.5:Kling 1.5`

- **Сервис (`services/kling.py`):**
  - Все методы (`generate_video_from_text`, `generate_video_from_image`, `generate_video_from_video`) теперь принимают параметр `model`
  - Модель передается в API запросе

- **Обработчики (`handlers/videos.py`):**
  - Добавлен шаг выбора модели в начале процесса
  - Порядок: **выбор модели → выбор длительности → загрузка контента → подтверждение → генерация**
  - Модель сохраняется в FSM и передается в API
  - Модель записывается в БД (`jobs.model`)

**Пример потока:**
1. Пользователь нажимает "🎬 Видео"
2. Выбирает "📝 Из текста"
3. Выбирает модель (например, "Kling 3.0")
4. Выбирает длительность (5 или 10 сек)
5. Отправляет текстовое описание
6. Видит экран подтверждения с моделью, длительностью и ценой
7. Подтверждает запуск

#### C) Исправлена кнопка "💰 Баланс"

**Проблема:** Кнопка "💰 Баланс" в главном меню не работала (не было обработчика).

**Решение:**
- **Обработчик (`handlers/balance.py`):**
  - Добавлен обработчик для текста "💰 Баланс"
  - Вынесена общая логика в функцию `show_balance_info()`
  - Теперь работают и команда `/balance`, и кнопка "💰 Баланс"

- **Удален дубликат (`handlers/common.py`):**
  - Удален неработающий обработчик кнопки баланса

### 📝 Дополнительные улучшения

- Улучшен UX: пользователь всегда видит стоимость операции перед подтверждением
- Добавлена возможность отмены на любом этапе
- Улучшена структура FSM состояний
- Обновлена документация (README.md, launch_instruction.md)

### 🔍 Технические детали

**Изменённые файлы:**
- `handlers/images.py` — полная переработка с добавлением подтверждения
- `handlers/videos.py` — полная переработка с выбором модели и подтверждением
- `handlers/balance.py` — добавлен обработчик кнопки баланса
- `handlers/common.py` — удален дубликат обработчика
- `services/kling.py` — добавлен параметр `model` во все методы
- `config.py` — добавлена загрузка моделей Kling
- `.env.example` — добавлена переменная `KLING_MODELS`
- `README.md` — обновлена документация
- `CHANGELOG.md` — создан этот файл

**Новые FSM состояния:**
- `ImageStates.confirming_generation` — подтверждение генерации изображения
- `ImageStates.confirming_edit` — подтверждение редактирования изображения

**Новые callback_data:**
- `image_generate_confirm` — подтверждение генерации изображения
- `image_edit_confirm` — подтверждение редактирования изображения
- `image_cancel` — отмена операции с изображением
- `video_model_{model_id}_{mode}` — выбор модели для видео
- `video_confirm_{mode}` — подтверждение генерации видео
- `video_cancel` — отмена операции с видео
- `video_back_main` — возврат в главное меню видео

---

## [v1.0] - 2026-02-11

### 🎉 Первый релиз

- Реализован базовый функционал бота
- Модуль изображений (Nano Banana Pro)
- Модуль видео (Kling)
- Система баланса
- Админ-панель
- База данных SQLite
- Асинхронная архитектура
