# Настройка автоматической оплаты через ЮКассу

## 📋 Требования

Для работы автоматической оплаты необходимо:

1. **Аккаунт в ЮКассе** — зарегистрируйтесь на [yookassa.ru](https://yookassa.ru)
2. **Магазин в ЮКассе** — создайте магазин в личном кабинете
3. **API ключи** — получите `shopId` и `secretKey` в настройках магазина
4. **Публичный URL** — для получения webhook уведомлений (опционально, но рекомендуется)

## 🔧 Настройка

### 1. Получение API ключей

**Войдите в личный кабинет ЮКассы** и перейдите в раздел **Настройки → Технические настройки**.

Вам понадобятся:
- **shopId** (Идентификатор магазина) — например, `123456`
- **Секретный ключ** — например, `live_abc123def456...`

### 2. Настройка переменных окружения

Откройте файл `.env` и добавьте настройки ЮКассы:

```ini
# YooKassa Payment Settings
YOOKASSA_SHOP_ID=123456
YOOKASSA_SECRET_KEY=live_abc123def456...
YOOKASSA_RETURN_URL=https://t.me/your_bot_username
```

**Параметры:**
- `YOOKASSA_SHOP_ID` — идентификатор магазина из личного кабинета
- `YOOKASSA_SECRET_KEY` — секретный ключ из личного кабинета
- `YOOKASSA_RETURN_URL` — URL для возврата после оплаты (ссылка на бота)

### 3. Установка зависимостей

Установите библиотеку YooKassa:

```bash
pip install yookassa==2.3.0
```

Или установите все зависимости из `requirements.txt`:

```bash
pip install -r requirements.txt
```

### 4. Настройка webhook (опционально, но рекомендуется)

**Webhook** позволяет автоматически получать уведомления о статусе платежей без необходимости ручной проверки.

#### 4.1. Получение публичного URL

Ваш бот должен быть доступен из интернета. Варианты:

**Вариант A: VPS/Dedicated сервер**
- Используйте nginx для проксирования запросов
- Настройте SSL сертификат (Let's Encrypt)

**Вариант B: Ngrok (для тестирования)**
```bash
ngrok http 8080
```

Вы получите URL вида: `https://abc123.ngrok.io`

#### 4.2. Настройка webhook в ЮКассе

Перейдите в **Настройки → HTTP-уведомления** в личном кабинете ЮКассы.

Добавьте URL для уведомлений:
```
https://your-domain.com/webhook/yookassa
```

Или для ngrok:
```
https://abc123.ngrok.io/webhook/yookassa
```

**Важно:** Выберите события для отправки:
- ✅ `payment.succeeded` — успешная оплата
- ✅ `payment.canceled` — отмена платежа

#### 4.3. Запуск webhook сервера

Webhook сервер запускается автоматически вместе с ботом. По умолчанию используется порт `8080`.

Для изменения порта отредактируйте `main.py`:

```python
# Запуск webhook сервера (если настроен)
if config.YOOKASSA_SHOP_ID and config.YOOKASSA_SECRET_KEY:
    webhook_runner = await setup_webhook_server(port=8080)  # Измените порт здесь
```

## 🚀 Использование

### Пополнение баланса

Пользователь может пополнить баланс командой `/pay`:

1. Бот показывает меню с предустановленными суммами (100, 500, 1000, 2000 ₽)
2. Пользователь выбирает сумму или вводит свою (от 100 до 15000 ₽)
3. Бот создает платеж в ЮКассе и отправляет ссылку на оплату
4. Пользователь переходит по ссылке и оплачивает
5. После успешной оплаты баланс автоматически пополняется

### Проверка статуса платежа

Если webhook не настроен, пользователь может проверить статус платежа вручную:

- Нажать кнопку **"🔄 Проверить оплату"** под сообщением с платежом
- Бот проверит статус через API ЮКассы
- Если платеж успешен, баланс будет пополнен

### Автоматическое пополнение (с webhook)

При настроенном webhook:

1. Пользователь оплачивает платеж
2. ЮКасса отправляет уведомление на ваш webhook URL
3. Бот автоматически пополняет баланс
4. Пользователь может сразу использовать средства

## 🔍 Проверка работы

### Тестовый режим

ЮКасса предоставляет тестовый режим для проверки интеграции:

1. В личном кабинете переключитесь на **Тестовый режим**
2. Используйте тестовые API ключи
3. Для тестовых платежей используйте карту: `5555 5555 5555 4477`

### Логи

Все операции с платежами логируются. Проверьте логи для отладки:

```bash
# Запуск бота с выводом логов
python main.py
```

Логи содержат:
- Создание платежей
- Проверку статусов
- Обработку webhook уведомлений
- Ошибки при работе с API

## ⚠️ Важные замечания

### Безопасность

**Секретный ключ** — это конфиденциальная информация:
- ❌ Не публикуйте его в открытых репозиториях
- ✅ Храните в `.env` файле
- ✅ Добавьте `.env` в `.gitignore`

### Комиссии

ЮКасса взимает комиссию за обработку платежей:
- Обычно 2-3% от суммы платежа
- Комиссия списывается с вашего счета в ЮКассе
- Пользователь платит полную сумму без комиссии

### Минимальная и максимальная сумма

В коде установлены ограничения:
- **Минимум:** 100 ₽
- **Максимум:** 15000 ₽

Для изменения отредактируйте `handlers/balance.py`:

```python
if amount < 100:  # Измените минимум
    await message.answer("⚠️ Минимальная сумма пополнения — 100 ₽")
    return

if amount > 15000:  # Измените максимум
    await message.answer("⚠️ Максимальная сумма пополнения — 15000 ₽")
    return
```

## 🆘 Решение проблем

### Ошибка "YooKassa не настроена"

**Причина:** Не указаны API ключи в `.env`

**Решение:**
1. Проверьте наличие файла `.env`
2. Убедитесь, что `YOOKASSA_SHOP_ID` и `YOOKASSA_SECRET_KEY` заполнены
3. Перезапустите бота

### Webhook не работает

**Причина 1:** URL недоступен из интернета

**Решение:** Проверьте доступность URL через браузер или curl:
```bash
curl -X POST https://your-domain.com/webhook/yookassa
```

**Причина 2:** Неверный URL в настройках ЮКассы

**Решение:** Проверьте URL в личном кабинете ЮКассы

**Причина 3:** Webhook сервер не запущен

**Решение:** Убедитесь, что в логах есть сообщение:
```
Webhook сервер запущен на порту 8080
```

### Платеж создается, но баланс не пополняется

**Причина:** Webhook не настроен или не работает

**Решение:**
1. Настройте webhook (см. выше)
2. Или попросите пользователя нажать **"🔄 Проверить оплату"**

## 📚 Дополнительные ресурсы

- [Документация YooKassa API](https://yookassa.ru/developers/api)
- [Python SDK для YooKassa](https://github.com/yoomoney/yookassa-sdk-python)
- [Личный кабинет ЮКассы](https://yookassa.ru/my)
- [Поддержка ЮКассы](https://yookassa.ru/docs/support)

## 💡 Рекомендации

Для продакшн использования рекомендуется:

1. **Использовать webhook** для автоматического пополнения баланса
2. **Настроить SSL** для безопасной передачи данных
3. **Мониторить логи** для отслеживания проблем
4. **Тестировать** в тестовом режиме перед запуском
5. **Резервное копирование БД** для сохранности данных о платежах
