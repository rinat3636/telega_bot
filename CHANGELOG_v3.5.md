# Changelog v3.5.0

## 🚀 Новые возможности

- **Реальные API интеграции:**
  - Вся конфигурация API (base_url, endpoints) вынесена в `config.py`
  - Добавлена поддержка реальных эндпоинтов для NanoBanana и Kling

- **Job-based протокол для Kling:**
  - Реализован асинхронный протокол: создание задачи → получение task_id → polling статуса
  - Поддержка статусов: `queued`, `processing`, `succeeded`, `failed`
  - Таймаут polling с автоматической отменой задачи

- **Безопасная передача файлов:**
  - `download_photo()` и `download_video()` теперь возвращают `bytes` вместо URL с токеном
  - Добавлена поддержка S3 для загрузки файлов и получения pre-signed URL
  - `get_file_for_api()` автоматически выбирает метод (bytes или S3) на основе `FILE_UPLOAD_METHOD`

- **Retry логика:**
  - Добавлена retry логика с exponential backoff и джиттером для 429/5xx ошибок
  - Распространяется на все API вызовы

## 🐛 Исправления

- **Утечка bot token:** Устранена утечка bot token при передаче URL в API
- **Надежность:** Повышена надежность API интеграций за счет retry и job-based протокола

## 🧪 Тесты

- Добавлены unit-тесты для новых API интеграций в `tests/test_api_integrations.py`
- Покрытие: успешные кейсы, retry, polling, обработка ошибок, bytes/URL
